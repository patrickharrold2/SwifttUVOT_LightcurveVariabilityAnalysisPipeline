# SwifttUVOT_LightcurveVariabilityAnalysisPipeline
A pipeline that is designed to run in windows shell for linux (WSL) terminal using the HEASFOT package through conda. Other appropriate package installations required.
